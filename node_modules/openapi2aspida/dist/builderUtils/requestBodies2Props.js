"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
var converters_1 = require("./converters");
exports.default = (function (bodies) {
    return bodies &&
        Object.keys(bodies)
            .map(function (defKey) {
            var _a;
            var target = bodies[defKey];
            var value;
            if ((0, converters_1.isRefObject)(target)) {
                value = (0, converters_1.$ref2Type)(target.$ref);
            }
            else {
                var content = target.content &&
                    ((_a = Object.entries(target.content).find(function (_a) {
                        var _b = __read(_a, 1), key = _b[0];
                        return key.startsWith('application/');
                    })) === null || _a === void 0 ? void 0 : _a[1]);
                if (!content)
                    return null;
                var result = (0, converters_1.schema2value)(content.schema, false);
                if (!result)
                    return null;
                value = result;
            }
            return { name: (0, converters_1.defKey2defName)(defKey), value: value };
        })
            .filter(function (v) { return !!v; });
});
//# sourceMappingURL=requestBodies2Props.js.map