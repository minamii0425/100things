generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Comment {
  id      Int    @id @unique @default(autoincrement())
  author  String
  comment String
}

model Todos {
  id Int @id @unique @default(autoincrement())
}
